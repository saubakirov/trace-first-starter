# CRUE assurance fixtures

These small text fixtures model a receiver-facing update receipt and a preserved legacy source. They
are intentionally free of credentials and are used only by `test_update_experience.py`.
