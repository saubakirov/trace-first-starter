# Проект

Активная редакция: Assisted
Версия редакции: 1.0
Версия папки: смотри файл `VERSION`. Что изменилось между версиями — в `CHANGELOG.md`
Состояние: НЕ ИНИЦИАЛИЗИРОВАН

> Строки редакции сохраняются дословно как устойчивые маркеры совместимости для ручной проверки и version-specific обновления. Версия папки живёт отдельной строкой и в файле `VERSION`.

Этот файл хранит устойчивый контекст именно этого проекта. При первом запуске Codex заполняет его из ответов пользователя; универсальная цель TFW не подменяет цель проекта.

Нейтральный starter не знает этот проект. Заполните карточку, ценности, ограничения, аудиторию и признаки успеха из ответов пользователя; не переносите готовый пример как факт.

## Карточка проекта

| Поле | Текущее состояние |
|---|---|
| Название | НЕ УКАЗАНО — заполните при инициализации |
| Участники | Будут созданы динамически при инициализации по полному имени и двум ролям; human identifier нового профиля строится из подтверждённой фамилии |
| Для кого создаются результаты | НЕ УКАЗАНО — назовите реальную аудиторию проекта |
| Цель | Впишите свою. Пример: сократить путь от решения до готового документа с недели до дня |
| Желаемый эффект | Впишите свой. Пример: возвращаясь к задаче через месяц, не восстанавливать контекст заново |
| Признаки успеха | Впишите свои. Пример: помощник отвечает по файлам папки и называет источник |
| Основной язык | Русский |

## Ценности и рабочие принципы проекта

Этот раздел не заполнен: рабочие ценности и принципы должны быть подтверждены для конкретного проекта.
> Не оставляйте универсальные или демонстрационные формулировки вместо ответа пользователя.
**Ценности проекта: НЕ УКАЗАНЫ.**
Запишите только явно подтверждённые ценности и поясните, как каждая влияет на решения:
- **Ценность 1:** НЕ УКАЗАНА.
- **Ценность 2:** НЕ УКАЗАНА.
- **Ценность 3:** НЕ УКАЗАНА.
- **Ценность 4:** НЕ УКАЗАНА.

**Три правила, которые важнее целей:**

1. ИИ — рабочий инструмент. Он делает механику; вопросы, решения и ответственность за результат остаются за человеком.
2. Порядок ИИ усиливает, беспорядок — тоже. На наведённом порядке он даёт скорость, без него ускоряет хаос.
3. Настоящие секреты в работу с ИИ не попадают: пароли, платёжные данные, персональные данные людей, финансовые документы.

## Образ полезного результата

Полезный результат в этом проекте определяется при инициализации и затем проверяется по его критериям:

- он опирается на файлы папки, и в нём видно, откуда взято каждое утверждение;
- он доведён до пригодного вида, а не оставлен заготовкой «допишу руками»;
- он оформлен в согласованном для проекта виде, а не по вкусу модели;
- рядом с ним лежит след: зачем делали, что решили по ходу, что проверили.

Помимо этого TFW требует явного пути к результату, проверки по критериям и достаточного следа для продолжения без пересказа чата.

## Участники и устройства

Профили людей и автоматизаций хранятся отдельными файлами в `team/` и обнаруживаются динамически; схема описана в `team/README.md`. При инициализации создаются только подтверждённые human-профили по полному имени с фамилией и двум ролям. Отсутствующая фамилия или коллизия требует уточнения; существующий профиль не перезаписывается.

Общий проект не хранит «текущего пользователя». После инициализации выбор устройства лежит в `%LOCALAPPDATA%\tfw\assisted\bindings.yml` на Windows или `~/.tfw/assisted/bindings.yml` на POSIX и адресуется стабильным `project_id`. Full `bindings.yaml` и legacy `tfw-assisted/bindings.yml` не читаются и не изменяются Assisted. Общий компьютер спрашивает участника в каждом новом человеческом чате.

Профиль и локальный выбор сообщают заявленного автора записи, но не подтверждают личность или полномочия. `project_id` генерируется только при инициализации конкретного проекта; starter не содержит общего или package ID.

## Ограничения

**Проектные ограничения: НЕ УКАЗАНЫ — заполните при инициализации.**
Не переносите в этот раздел ограничения, факты или политику другого проекта.
- **Хранилище и синхронизация:** укажите фактическую среду и правила shared writes.
- **Чувствительные сведения:** определите, что нельзя сохранять в проекте или общей памяти.
- **Проверенные сведения:** храните подтверждённые факты в `knowledge/records/` и обновляйте их отдельным решением.
- **Высокорисковые решения:** назовите области, где ИИ не делает выводы или действия без специалиста.
- **Внешние действия:** отправка, публикация, оплата и удаление требуют подтверждения человеком.

**Всегда действуют границы Assisted:**

- секреты не записываются в проект;
- чувствительные сведения не переходят в общую память без явного подтверждения;
- одну задачу и один изменяемый trace ведёт один владелец записи;
- одновременное редактирование одного общего файла не считается безопасным;
- периодическая консолидация памяти и организационные режимы не входят в эту редакцию.
