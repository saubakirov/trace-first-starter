interface:
  display_name: "TFW Assisted Update"
  short_description: "Агентское обновление с явным gate"
  default_prompt: "Используй /tfw-update: сравни и классифицируй все пути, покажи полный read-only план, остановись на одном gate, затем повторно проверь baseline и сохрани downstream-состояние."
policy:
  allow_implicit_invocation: true
