# What changed for you — TFW {from-version} → {to-version}

> **Project**: {project name} · **Date**: YYYY-MM-DD · **Written in**: `tfw.content_language`
> **Derived from**: the CHANGELOG entries between the two versions — nothing else

## What is now possible

<!-- one bullet per `Added` item across the intervening entries, in the owner's language -->
- {…}

## What you now do differently

<!-- one bullet per `Changed` item; a reversal names the old rule and the new one -->
- {…}

## What stopped breaking

<!-- one bullet per `Fixed` item -->
- {…}

## What no longer has to be done

<!-- one bullet per `Removed` item -->
- {…}

<!--
CANONICAL TEMPLATE — the update's LAST message (update.md Step 8a). Copy it, fill the four
blocks, deliver it to the owner, record in the update checklist that it was delivered.

FOUR BLOCKS, FOUR CHANGELOG SECTIONS, and nothing else:

    Added    →  What is now possible
    Changed  →  What you now do differently
    Fixed    →  What stopped breaking
    Removed  →  What no longer has to be done

Read every entry between the installed version and the target. Each bullet here is bound to a
bullet there: same fact, the owner's language, the owner's terms — what it lets them do, not
what the procedure calls it ("you can run two tasks at once without meeting in one file", not
"provenance drift is distinguished from customization").

A SECTION THE ENTRIES DO NOT CARRY yields one line in its block: *nothing in this release*.
Never a bullet the CHANGELOG does not have. `Why this release exists`, `Verification`,
`Known open` and `Canon` are not read: they explain the release to its authors, not the change
to its owner.

NO FREE TEXT. The structure is the whole of the positive framing the owner asked for —
possibilities, then the habits that change, then repairs, then obligations that fall away — so
the template fixes the structure and the CHANGELOG supplies every fact. A sentence with no
CHANGELOG bullet behind it is the improvisation this file replaces.

WHY IT EXISTS. An owner returned to a project after an unattended update and was told, in the
procedure's words, what had changed — "drift", "allowlist" — and nothing about what had become
possible. Their words: explain what changed, positively, because people do not like change.
This is the shape of that explanation, so it is delivered every time and reads the same way in
every project.
-->
