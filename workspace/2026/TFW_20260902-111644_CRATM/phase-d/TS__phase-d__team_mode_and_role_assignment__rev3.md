# TS — TFW_20260902-111644_CRATM / Phase D: Team mode and Role Assignment, revision 3

> **Date**: 2026-09-06
> **Author**: Codex (Phase D Coordinator, acting as `saubakirov`)
> **Status**: 🟡 TS_DRAFT — awaiting exact Main Coordinator approval
> **Parent HL**: [current derivative Phase HL](HL__phase-d__team_mode_and_role_assignment.md)
> **Master contract**: [owner-approved A7](../HL-TFW_20260902-111644_CRATM.md) at `2386bfb0994f6e0a1aed7b734e345cdb2a540ae1`
> **Predecessor**: [approved TS revision 2](TS__phase-d__team_mode_and_role_assignment__rev2.md), approval epoch `25930c5238036b3d96ee5ae50d195a9dc340f394`
> **Live return source**: [REVIEW revision 2](REVIEW__phase-d__team_mode_and_role_assignment__rev2.md) plus Main's pre-Candidate A7 consumer finding
> **Round**: 3 — the complete rev2 order stands except where this sibling explicitly replaces or extends it

---

## 1. Objective

Finish the already-approved A7 correction and close one material navigation consumer gap before an
immutable Candidate exists. Only the owner-selected stable agent principal's exact root Coordinator
unit may render `LEAD`; that root keeps one handle-bearing LEAD navigation form across PLAN and RESUME,
while every child retains its ordinary workflow cue. This adds no principal, profile, grant, authority,
runtime or bulk rename and preserves the same Executor's tested work in place.

## 2. Scope

### Source separation

1. **Required A7 consistency correction (A).** Frozen A7 distinguishes the selected principal from
   its root Coordinator and every child unit. Master §7 P3/P5/P6 says initiation starts at a
   Coordinator, acting on behalf of the named principal does not distribute its grant, and a title
   grants nothing. Rev2 AC-2 allows only the selected LEAD/root Coordinator to consume the grant and
   AC-4 requires consumers to resolve the actual unit. The live `Session identity` rule instead maps
   `PLAN`/`RESUME` plus a generic governing bound to `LEAD`, so a bounded same-principal child can be
   displayed as the accountable root. A is a TS defect inside approved A7, not a new frozen claim.
2. **Requested navigation refinement (B).** The owner's latest Main input is: *“lead может быть только
   именной агент, я бы даже имя выводил в сессию типа lead name и т.д.”* This requests visible named-LEAD
   navigation but does not itself choose a technical field or alter authority. Main, under the standing
   technical coordination mandate, selects the exact stable `team/{handle}.md` handle rather than the
   mutable display `name`. B is an ergonomic refinement, distinct from A7's authority correction.
3. **No direct-human grammar verdict is fabricated.** A7 remains the authority for A; the quoted owner
   input plus Main's field choice governs B. The title is navigation-only and creates no profile,
   identity, mandate, dispatch edge, role permission or amendment right.

### Live lineage and WIP preservation

- Rev2 reached `ONB` at `93359293797b97d0e2a87e303a432ba8f00d7e5c`. Before Candidate, the same
  Executor stopped with exactly 18 VALUE plus 2 ASSURANCE paths uncommitted and no untracked files.
- Product Baseline-to-WIP measured `420` additions + `446` deletions = `866` touched text LOC across
  the exact rev2 18-path VALUE selector. This is observed non-Candidate input, not retrospective
  approval of an `18/866` plan and not a replacement for the earlier `18/760` forecast.
- Because the graph has no `ONB → TS_DRAFT` edge, the Executor recorded the real missing-approved-order
  dependency by legal `ONB → BLOCKED` TRACE commit
  `c1809cd4a9c85c8dbc83e5c31fc6daa797debd29`. No DONE exception, RF or REV was invented.
- This Coordinator fast-forwarded a clean planning worktree to that exact head. This draft changes
  only derivative Phase HL, TS rev3, phase state and its new transition event. After exact approval,
  the Executor may fast-forward only when those incoming TRACE paths do not overlap its 20 WIP paths.
  Record `git hash-object` (or equivalent Git object hashes) for each dirty file immediately before
  and after the fast-forward; both the exact path set and every content hash must match. No reset,
  force, implicit stash, blanket commit or overwrite is allowed.
- RTBO independently landed saved-master at `ae494e2a9f9ee82e5d0bd2a9d79e4e23d58a1822` with
  overlapping canon and assurance changes; Main verified that tracked/index state is clean. It does
  not change this task's immutable Product Baseline
  or current WIP. Before eventual CRATM landing, Main must compare the actual RTBO RF/REVIEW and new
  canon, must not restore removed `gen_index.py`/tracked `workspace/00-INDEX.md`, and must allocate a
  free decision number rather than rewriting old D82. That is an integration check, not Candidate work.

### In Scope

- Carry forward every rev2 requirement for A7 mandate/unit separation, proposal origin, replacement,
  supplied-versus-additional provider admission, copy parity, two-epoch history and A5 evidence.
- Replace `Session identity` LEAD qualification with an exact source-derived conjunction:
  `WORK ∈ {PLAN, RESUME}`; authoritative governing lineage selects one valid agent principal as LEAD;
  the acting principal is that selected handle; and the current actual unit is the mandate's exact root
  Coordinator unit. Every fact must resolve before the label is attempted.
- Render a qualified root as `LEAD · {handle} · {TASK}[ · {PHASE}]`, using the exact stable profile
  handle. The same root retains that form across Plan and Resume. Existing TASK, PHASE, collision,
  exact-readback and report-once/fail-soft rules remain.
- Keep a child at ordinary `PLAN · …` or `RESUME · …` even when it shares the selected principal,
  has a valid bound, or itself holds a Coordinator role. A human principal, unselected/different agent,
  stale handle, forwarded selection, wrong role, or missing/ambiguous root/current-unit fact cannot
  emit `LEAD` or a guessed handle.
- Update the canonical Plan and Resume consumers and their accepted `.agent`/`.claude` copies. Extend
  only the two existing ASSURANCE modules with source-derived records, scenarios, negative cases and
  output-changing mutants. Expand the literal VALUE selector by exactly the three Resume paths.
- Optionally give the Codex adapter/root managed block one compact provider-facing sentence for the
  exact handle-bearing root title; both paths already belong to the selector. Do not rename any live
  task as product evidence. `CRATM — Main Coordinator` remains the owner's explicit live-task
  navigation exception; no principal is created merely to rename it.

### Out of Scope

- Editing approved TS rev2, master A7, REVIEW rev2, old journal/review/RF/EV content, Phase A–C,
  release 2.2.0, Phase E, KNOWLEDGE before APPROVE, or any protected approval-era blob.
- A new principal/profile/binding, per-role name, mutable display-name title, human `LEAD`, inferred
  handle, authority-bearing title, runtime, liveness, roster, carrier, config/event key or artifact.
- Bulk, retroactive or evidence-only task renaming; changing title separators, collision suffixes,
  TASK/PHASE derivation, readback behavior, or the cues of Research/Handoff/Review/Docs/Init.
- New provider trial/profile, G8 claim, bridge, relay, hidden helper, new Executor/Reviewer, release,
  tag, push or saved-master integration.

## 3. What stands from revision 2

Rev2 §§1–3, AC-1 through AC-5, technical guidance, and failure/risk rules remain binding unless an
exact clause below replaces them. In particular:

- A7 still selects one stable named LEAD principal; working units stay distinct, direct and human-rooted.
- Proposal origin, child grant non-inheritance, no self-ruling, owner reservations and §12 `SUPERSEDE`
  replacement rules do not change.
- Role Assignment retains separate protected-mandate and dynamic-unit layers.
- Plan/Handoff/Research/Review, their original eight copies, and Codex operations retain the rev2 model.
- The supplied Codex profile remains the one disclosed G1–G7/no-G8 exception; every additional profile
  still requires one native G1–G8 unit, and partial evidence never composes.
- Product Baseline `8e68ab37d300122ff110500ad58f354f76b6210f`, historical denominator
  `16/640`, thresholds `32/1,280`, Candidate timing and two-epoch protection remain immutable.

## 4. Principles Check

| # | Master/PV item | Enforced by | Gate |
|---|---|---|---|
| P1 | Human authority, bounded delegation | AC-7, HC-D5 | Owner-selected agent/root facts precede LEAD label; title grants nothing |
| P3 | Every initiation edge starts at a Coordinator | AC-7 | Root-unit equality; a child Coordinator is still a child |
| P5–P7 | Name carries principal grant; role/title grants nothing | AC-7, mutants | Handle identifies the selected principal but never substitutes for unit or authority |
| P10–P11 | Session is not a person; autonomy is explicit | AC-7 | Current unit and selected principal are separately sourced |
| PV1 | Structural Enforcement; Naming Creates Behavior | AC-7 | Exact predicate and title grammar are source-derived and mutation-tested |
| D79–D82 | Navigation, identity, delegation and AT activation compose | AC-6, AC-7 | Plan/Resume consumers and accepted copies agree with A7 |
| process F39–F41 | Complete selector; source-written order; rationale-led bound | §5 | Definition, consumers, tests and protected non-consumers are enumerated |

## 5. Affected Files and Value-Bearing Accounting

### Literal VALUE selector and cumulative Baseline forecast

| File | Action | Class | Planned + | Planned − | Semantic reason / required result |
|---|---|---:|---:|---:|---|
| `.tfw/conventions.md` | MODIFY | `VALUE` | 122 | 11 | Rev2 contract plus exact LEAD root predicate and handle-bearing grammar |
| `.tfw/templates/HL.md` | MODIFY | `VALUE` | 31 | 0 | Rev2 two-layer mandate/unit form |
| `.tfw/templates/team/profile.md` | MODIFY | `VALUE` | 9 | 19 | Rev2 principal/grant consumption |
| `.tfw/templates/journal/event.md` | MODIFY | `VALUE` | 6 | 0 | Rev2 unit/proposal dispatch provenance |
| `.tfw/workflows/plan.md` | MODIFY | `VALUE` | 36 | 58 | Rev2 Plan duties plus exact root/handle resolution |
| `.tfw/workflows/resume.md` | MODIFY | `VALUE` | 4 | 2 | Preserve root LEAD across Resume; children keep RESUME |
| `.tfw/workflows/handoff.md` | MODIFY | `VALUE` | 13 | 18 | Rev2 Executor unit consumption; session cue unchanged |
| `.tfw/workflows/review.md` | MODIFY | `VALUE` | 13 | 11 | Rev2 Reviewer/origin return; session cue unchanged |
| `.tfw/workflows/research/base.md` | MODIFY | `VALUE` | 23 | 52 | Rev2 Researcher unit consumption; session cue unchanged |
| `.tfw/adapters/codex/AGENTS.md.template` | MODIFY | `VALUE` | 13 | 9 | One handle-bearing root LEAD task; distinct child tasks; rev2 admission |
| `AGENTS.md` | MODIFY | `VALUE` | 13 | 9 | Exact managed receiver; preserve bytes outside markers |
| `.agent/workflows/tfw-plan.md` | MODIFY | `VALUE` | 36 | 58 | Exact accepted Plan copy |
| `.agent/workflows/tfw-resume.md` | MODIFY | `VALUE` | 4 | 2 | Exact accepted Resume copy |
| `.agent/workflows/tfw-handoff.md` | MODIFY | `VALUE` | 13 | 18 | Exact accepted Handoff copy |
| `.agent/workflows/tfw-review.md` | MODIFY | `VALUE` | 13 | 11 | Exact accepted Review copy |
| `.agent/workflows/tfw-research.md` | MODIFY | `VALUE` | 23 | 52 | Exact accepted Research copy |
| `.claude/commands/tfw-plan.md` | MODIFY | `VALUE` | 36 | 58 | Exact accepted Plan copy |
| `.claude/commands/tfw-resume.md` | MODIFY | `VALUE` | 4 | 2 | Exact accepted Resume copy |
| `.claude/commands/tfw-handoff.md` | MODIFY | `VALUE` | 13 | 18 | Exact accepted Handoff copy |
| `.claude/commands/tfw-review.md` | MODIFY | `VALUE` | 13 | 11 | Exact accepted Review copy |
| `.claude/commands/tfw-research.md` | MODIFY | `VALUE` | 23 | 52 | Exact accepted Research copy |
| **Total** | **21 MODIFY** | **`VALUE`** | **461** | **471** | **932 touched text LOC** |

The forecast uses the measured held WIP as its input: `420+446=866`, then budgets `+41/-25=66`
touched LOC for the combined A+B correction. It is a forecast, not a ceiling or retroactive Candidate.

### Declared excluded selectors

| File / selector | Action | Class | Treatment |
|---|---|---|---|
| `docs/scripts/test_runtime_context.py` | MODIFY | `ASSURANCE` | Source-derived LEAD/root/title records, exact scenarios and independent mutants |
| `docs/scripts/test_integration.py` | MODIFY | `ASSURANCE` | 21-path selector, Resume copy parity, protected non-consumers/history and Candidate epochs |
| Derivative Phase HL, TS rev3, status/journal and later cumulative ONB/EV/RF/REVIEW | CREATE/MODIFY | `TRACE` | Planning, legal re-entry, approval, execution, evidence and review history; never move Candidate |
| `.tfw/glossary.md` → `Session Naming` | NONE | protected `VALUE` | Still correctly delegates grammar to conventions; verify byte-exact from approval epoch |
| `.tfw/workflows/{docs,init}.md` and accepted copies | NONE | protected `VALUE` | Task-bound cues remain DOCS/INIT and cannot emit LEAD |
| `.tfw/templates/bindings.yaml` | NONE | protected `VALUE` | Binding remains attribution-only and never supplies the LEAD title predicate |
| Master HL, A–C, old D artifacts/events, KNOWLEDGE, knowledge, release and Phase E | NONE | protected history/prior `VALUE` | Match the new approval epoch; no rewrite or premature closure work |

### Prospective accounting contract

| Fact | Proposed value |
|---|---|
| Subject / exact VALUE selector | The twenty-one literal paths above, in that order; existing 18 plus exactly the canonical Resume workflow and its two accepted copies |
| Baseline / selector source | Product Baseline `8e68ab37d300122ff110500ad58f354f76b6210f`; A7 freeze; rev2; Main A/B planning decision; this TS at its future exact approval commit |
| Candidate rule | First tested Executor commit containing all 21 VALUE plus both ASSURANCE paths, descendant of the new approval epoch, before EV/RF/REVIEW/final transition; later VALUE requires replacement/recomputation |
| Logical VALUE files | `21`; all existing modified text paths; rename = one |
| Touched text LOC | `461` additions + `471` deletions = `932`; cumulative Baseline forecast, numeric numstat fields |
| Triggers / disposition | Configured prompts `50`/`5,000`: one phase because A7 authority, navigation definition, Plan/Resume consumers, accepted copies and assurance must agree; splitting A from B would duplicate the same predicate/test surface |
| Multiplier / authority | Historical `16/640` remains the immutable denominator; owner thresholds remain `32/1,280`. Proposed `21/932` is below both and does not ratchet them. Main exact approval is required before A/B implementation |
| Approval epoch / failure | Future commit containing exact Main approval metadata after review of this draft commit. It protects the complete draft, derivative HL and all earlier trace; missing/mutable/mismatched/late = BLOCKED |

```powershell
$valuePaths = @(
  '.tfw/conventions.md',
  '.tfw/templates/HL.md',
  '.tfw/templates/team/profile.md',
  '.tfw/templates/journal/event.md',
  '.tfw/workflows/plan.md',
  '.tfw/workflows/resume.md',
  '.tfw/workflows/handoff.md',
  '.tfw/workflows/review.md',
  '.tfw/workflows/research/base.md',
  '.tfw/adapters/codex/AGENTS.md.template',
  'AGENTS.md',
  '.agent/workflows/tfw-plan.md',
  '.agent/workflows/tfw-resume.md',
  '.agent/workflows/tfw-handoff.md',
  '.agent/workflows/tfw-review.md',
  '.agent/workflows/tfw-research.md',
  '.claude/commands/tfw-plan.md',
  '.claude/commands/tfw-resume.md',
  '.claude/commands/tfw-handoff.md',
  '.claude/commands/tfw-review.md',
  '.claude/commands/tfw-research.md'
)
$baselineSha = '8e68ab37d300122ff110500ad58f354f76b6210f'
if ($candidateSha -notmatch '^[0-9a-f]{40}$') { throw 'Candidate must be the full immutable Executor SHA' }
git diff --name-status --find-renames=50% -z $baselineSha $candidateSha -- $valuePaths
git diff --numstat --find-renames=50% -z $baselineSha $candidateSha -- $valuePaths
```

### Prospective scope rulings

1. **A — required A7 consistency.** Cause: generic `governing-bound` collapses root and child in a
   live consumer. Cost: three new VALUE members and part of the `+41/-25` increment. Assurance:
   source-derived principal/root/current-unit records plus predicate-collapse mutants. Split rejected:
   changing conventions without Plan/Resume and accepted copies leaves contradictory consumers.
2. **B — owner-requested navigation refinement.** Cause: make the one named LEAD visible without
   naming children. Main chooses stable profile handle, not mutable display name. Cost: no further
   selector member; it shares A's definition/consumer/test surface. Trade-off: titles are longer and
   expose a project-local handle, but remain non-authoritative and fail soft when unresolved. Display
   name, OS/account name and a human binding were rejected as mutable, inferred or the wrong identity.
3. **Observed rev2 WIP variance.** Rev2 planned `18/760`; held pre-Candidate WIP is `18/866`, a `+106`
   LOC deviation inside the same selector. It is disclosed and ruled prospectively here before further
   VALUE work; it created no Candidate and remains below `1,280`. The combined plan is `21/932`.
4. **A5 attention.** Existing over-limit documents may grow only by the minimum materially necessary.
   Current WIP words include conventions `12,047`, Plan `1,968` and Resume `458`; Plan and conventions
   require compact substitution, not duplicated exposition. Executor records exact before/after route,
   central-range, document and active-corpus measures, rejected shorter form and any crossing. No cap
   literal changes, test erasure or semantic deletion are allowed.

### Task-local hard constraints

Rev2 HC-D1–HC-D4 stand. Add:

| ID / M1 consequence | M2 object/risk | M3 measure/selector | M4 pre-act check | M5 softer-control gap | M6 change authority |
|---|---|---|---|---|---|
| HC-D5 — child appears as accountable LEAD | Navigation root/unit collapse | Selected principal, acting principal, root unit, current unit, workflow cue | Resolve all four from authoritative lineage before title attempt | `governing-bound` or shared attribution alone accepts the child | Main within A7; principal/authority change to owner |
| HC-D6 — WIP is laundered into approval | Candidate/epoch timing | Exact HEAD, 20 WIP paths, new approval SHA | No Candidate or VALUE commit before approval; fast-forward only on no overlap | A retrospective plan can hide the +106 variance | Main for 21/932; ≥32/1,280 to owner |

**Actions (not budget dimensions):** 21 VALUE modifications; 2 ASSURANCE modifications; one Phase HL
refresh, one TS sibling, legal `BLOCKED → TS_DRAFT` state/event, later cumulative ONB/EV/RF and new
REVIEW sibling TRACE; zero new product artifact, principal, profile, runtime, config key or Phase E path.
**Immutable owner-approved denominator:** historical `16` VALUE files / `640` touched LOC; never ratchets.

## 6. Acceptance Criteria

### AC-1 through AC-5: Revision 2 outcomes stand

Every rev2 AC-1–AC-5 checkbox, dependency, gate and evidence obligation remains required. The A/B
clauses below extend AC-2 and AC-4; they do not weaken mandate, origin, replacement or admission.

### AC-6: Replacement Candidate, history, WIP and attention are reproducible

- [ ] Exact Main approval fixes this TS content, the 21-path selector and `461+471=932` forecast before
  further VALUE work. Candidate is the first tested Executor commit after that epoch; Baseline replay
  returns exactly 21 VALUE members and numeric additions/deletions. Historical 16/640 never ratchets.
- [ ] Approval-epoch diff admits only 21 VALUE, 2 ASSURANCE and legal Phase D continuation TRACE. It
  protects master A7, A–C, rev1/rev2 D orders and reviews/stages, every earlier journal event, release,
  bindings, glossary, unaffected session-identity consumers, current derivative HL and approved rev3.
- [ ] The Executor's pre-approval WIP remains uncommitted across the Coordinator trace/plan/approval
  fast-forwards. Before/after dirty-file object hashes and the exact path set are identical. No
  overlap, extra/missing WIP path, cached path or untracked path is tolerated. The Candidate is not
  fabricated from the trace commit and contains the complete tested 21+2 result.
- [ ] ONB/RF/EV remain cumulative: preserve all earlier bytes and append one new numbered round section
  only at the legal stage. The next review is a new sibling after REVIEW rev2.
- [ ] Exact A5 and VALUE reports disclose the old `18/760` plan, observed `18/866` WIP, approved
  `21/932` plan and actual Candidate without presenting any forecast as actual.
- [ ] Targeted Phase B/C/D and session-identity checks, all required scenarios/mutants, copy/managed
  receiver parity, protected-history checks, `git diff --check`, full configured suite and real MkDocs
  build pass. Candidate stays reachable; no push/release/saved-master integration.

Gate: literal 21-path PowerShell replay; approval-epoch membership/blob queries; exact WIP path-set
and dirty-file object-hash maps before/after fast-forward; A5 reports; targeted/full tests and build.

Evidence: EV appends raw decoded selector/numstat, WIP preservation, ancestry, timing, history and A5
records with command exits.

### AC-7: Only the selected root renders the stable handle-bearing LEAD title [depends: AC-2] [depends: AC-4]

- [ ] `Session identity` defines LEAD from source-derived selected-agent-principal, acting-principal,
  root-Coordinator-unit and current-actual-unit equality, limited to PLAN/RESUME. Generic bound,
  attribution, role, title or provider facts are insufficient.
- [ ] Exact qualified form is `LEAD · {handle} · {TASK}[ · {PHASE}]`. `{handle}` comes from the selected
  valid agent profile and authoritative mandate, never mutable `name`, title, OS/account/provider,
  human binding or chat. Navigation remains non-authoritative.
- [ ] The same exact root in Plan and Resume retains the LEAD form. A same-principal bounded child
  Coordinator remains PLAN or RESUME; Human principal, unselected/different agent, stale profile
  handle, forwarded selection, wrong role, missing/ambiguous root and missing/ambiguous current unit
  all retain ordinary WORK and emit no handle.
- [ ] Existing TASK/PHASE, collision suffix, exact readback, report-once and fail-soft behavior remains.
  Research/Handoff/Review/Docs/Init cues and copies do not acquire LEAD or handle behavior. Glossary
  `Session Naming` remains byte-identical and semantically accurate.
- [ ] Canonical Plan/Resume equal their two accepted copies byte-for-byte. Codex managed block/root
  remain exact if the compact provider-facing sentence is used; bytes outside markers are unchanged.
- [ ] Independent mutants that reduce the root predicate to governing bound or principal match, treat
  child role as root, leak handle/title to a child, use mutable display name, accept stale/forwarded
  identity, or lose Plan↔Resume continuity must change output before rejection.

Gate: one independent source-derived oracle parses conventions, authoritative mandate/profile and
Plan/Resume consumers; runs root PLAN, root RESUME, same-principal child, human, different/unselected,
missing/ambiguous/stale/forwarded/wrong-role cases; then runs every required mutant and copy check.

Evidence: EV appends the full input record, exact intended title, actual/readback/fail-soft result,
source manifest and independent mutant decision for each case.

### Evidence Artifacts

| File | Description |
|---|---|
| `evidence/EV__phase-d__team_mode_and_role_assignment.md` | Cumulative rev3 evidence for rev2 ACs, AC-6 replacement and AC-7 navigation |

## 7. Technical Guidance

- Replace the fixture-only `lead_binding: bool` model with explicit facts. The test oracle must derive
  the result from selected principal, profile type/validity, root/current unit identity and workflow.
- Prefer one compact predicate in `Session identity`; Plan/Resume name the facts/checkpoint but do not
  duplicate the full grammar. This is the A5-shortest complete enforcement shape.
- Treat the handle as a displayed stable identifier only. Do not let title parsing become an authority,
  identity or current-unit source, and do not make a human profile eligible for LEAD.
- Extend the existing copy map with Resume rather than inventing a second parity mechanism. Verify the
  other five task-bound cues remain unchanged even though three of their files carry separate rev2 work.
- Before Executor fast-forward, compare its exact 20-path WIP set and incoming changed paths. Any real
  overlap, missing path, cached entry, untracked path, changed before/after dirty-file object hash or
  validation mismatch returns to Coordinator.

## 8. Definition of Failure

- ❌ A child renders LEAD because it shares principal, bound or Coordinator role with the root.
- ❌ A root renders LEAD without exact owner-selected valid agent principal and exact root/current-unit
  equality, or the handle is guessed/parsed from title/provider/OS/account/chat.
- ❌ PLAN and RESUME of the same root disagree on LEAD identity, or any other workflow cue gains it.
- ❌ A title grants authority, child amendment rights, profile identity, mandate or dispatch validity.
- ❌ Glossary/unaffected consumers drift, accepted copies disagree, or selector omits a definition,
  consumer or test while claiming completeness.
- ❌ The 18/866 WIP is committed before approval, overwritten, changes any dirty-file object hash
  across fast-forward, is stashed implicitly, called Candidate, or hidden as if 18/760 had been actual.
- ❌ Rev2/master/history is edited, an illegal lifecycle edge is used, or the prior DONE exception is
  generalized. The only current route is the recorded ONB→BLOCKED, this draft's BLOCKED→TS_DRAFT, and
  after approval the same Executor's TS_DRAFT→ONB acceptance.
- ❌ Any rev2 failure condition is weakened, including origin, replacement, admission, A5 or history.

**On failure:** stop before the act. Technical/selector/copy gaps return to this Phase Coordinator;
principal/authority, owner thresholds or lifecycle-policy changes return through Main to the owner.

## 9. Phase Risks

| Risk | Mitigation |
|---|---|
| Handle visibility is mistaken for authority | Navigation-only clause plus negative authority mutants |
| Same-principal child passes a weakened predicate | Exact current-unit=root-unit equality and collapse mutants |
| Mutable/stale profile data produces a false title | Exact valid selected handle or ordinary WORK fail-soft |
| Resume is missed because rev2 changed only four workflows | Three explicit new VALUE members and copy parity |
| WIP/forecast history is laundered | Four separately named figures: 18/760, 18/866, 21/932, actual Candidate |
| A5 grows already long sources | Compact central predicate, consumer pointers, exact before/after evidence |
| Independent RTBO landing overlaps canon/tests | Keep current baseline/WIP; Main performs post-review RF/REVIEW consistency check and never restores retired runtime/index |

## 10. Cross-Phase Modifications

| File | Also modified in | Coordination note |
|---|---|---|
| `.tfw/conventions.md` | CRATM A–D; RTPSN B; future E | Compose D79 navigation with A7 without changing historical results |
| Plan/Resume canonical and accepted copies | RCFR/RTPSN; CRATM D | Root-only LEAD mapping; other workflow cues remain unchanged |
| Codex adapter/root managed block | CRATM D; future E | Optional compact handle-bearing root instruction; no task rename/profile |
| `docs/scripts/test_{runtime_context,integration}.py` | RCFR/RDP/VBSA/RTPSN; CRATM A–D | Extend existing assurance and retain all earlier entrypoints |
| `KNOWLEDGE.md` §§1–3 | CRATM A–D docs history | Protected during Candidate; `/tfw-docs` only after new APPROVE |

> References: master A7; D79–D82; `knowledge/process.md` F39–F41; rev2; Executor TRACE commits
> `93359293797b97d0e2a87e303a432ba8f00d7e5c` and `c1809cd4a9c85c8dbc83e5c31fc6daa797debd29`.

---

*TS — TFW_20260902-111644_CRATM / Phase D: Team mode and Role Assignment, revision 3 | 2026-09-06*
