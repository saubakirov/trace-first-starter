# Briefing — "What should we investigate?"
> **Mindset:** Strategist. You're planning an investigation, not doing it. Frame what matters. Resist solving.
> **Test:** "Can I explain WHY we're investigating this and what would change our approach?"
> Parent: [HL-TFW_20260830-202031_FA15ES](../../HL-TFW_20260830-202031_FA15ES.md)
> Goal: Assisted 1.6 remains a faithful independent TFW realization while Full and Assisted coexist on one device and publishers choose update sources without provider-specific product machinery.

## Research Plan

### Gather

- Re-open iteration 1 decisions D1–D8 and its four open threads; treat B2 and the transport-neutral updater as claims to falsify, not as settled implementation instructions.
- Decompose the remaining choice into binding location, legacy-state handling, host portability, source representation, byte identity, recheck semantics, origin assurance, and documentation ownership.
- Run an in-memory state fixture for Full/new-Assisted/legacy-Assisted presence, validity, foreign lock, shared-device choice, and autonomous role; require zero reads or writes to the Full file from Assisted cases.
- Inspect authoritative Windows, Linux/XDG, macOS, Google Drive, GitHub, URI/hash, and software-update security documentation, plus the read-only field Assisted 1.6 lineage.

### Extract

- Cross-reference the Gather dimensions into binding and update-source configuration families without evaluating them prematurely.
- Resolve the exact Windows and explicit POSIX paths and compare B2 with platform-native Assisted placement and a separate common root.
- Build a Drive/GitHub/local matrix covering exact object, digest, dynamic manifest, pre-gate recheck, mutable locator behavior, and provider-neutral normalization into one closed tree.
- Map the minimum authoritative documentation locus for publisher shelf, consumer acquisition behavior, migration semantics, and maintainer direction.

### Challenge

- Inject stale legacy state, both-present disagreement, invalid new state, foreign lock, shared computer, and autonomous-role cases; verify fail-closed/session-only behavior and one-time re-onboarding boundaries.
- Re-read the field source and remote metadata to test no-drift behavior, then simulate same-version byte substitution to distinguish an observed digest from a trusted expected digest.
- Attack every surviving update configuration with mutable-location, incomplete-package, and origin-substitution cases; state the exact residual-risk boundary rather than implying authentication.
- Attempt to remove each proposed documentation edit or assurance signal; keep only loci and controls with a unique job.

### Synthesis

- Decide H2a and H4 against the observed fixtures and explicit Windows/POSIX/provider matrices.
- Record any remaining gaps precisely and classify the iteration as `SUFFICIENT`, `MORE NEEDED`, or `BLOCKED` without changing the HL or iteration control.
- Separate free-section refinements from any frozen-contract amendment proposals and carry no product implementation beyond research evidence.

## Hypotheses (from HL §10)

| # | Hypothesis | HL Status |
|---|-----------|-----------|
| H2a | One backward-compatible machine-local bindings facility can serve Full and Assisted concurrently while keeping their files or namespaces, project keys, profile schemas, and write protocols independent and leaving Full product files unchanged. | conditionally supported — iteration 1 selected an unchanged Full file plus an Assisted-owned child namespace and one-time re-onboarding; fixture and POSIX validation remain |
| H4 | A provider-neutral source contract can let each Assisted producer select its own release location while the prompt-only `/tfw-update`, version, changelog, and migration maps keep updates explicit and human-gated; research must determine whether a separate manifest or schema has a unique necessary job. | supported in design — iteration 1 found no unique job for a static source manifest; Drive/GitHub/local fixtures and the origin-authentication boundary remain |

## Predecessor Context

Iteration 1 [RES](../iter1/RES.md) established D1–D3: separate Full and Assisted files, provisional B2 paths, and one-time re-onboarding with no legacy migration or fallback. It also established D4–D7: provider neutrality belongs to acquisition behavior; a publisher documents its release shelf; an invocation resolves one exact package; integrity/version/migration/evidence remain separate; and the rejected static JSON pair has no unique in-scope job. D8 kept the research open pending fixtures.

The predecessor's open threads are: (1) binding path and one-time re-onboarding states, (2) Drive/GitHub/local exact-object and recheck behavior, (3) the origin-authentication residual-risk boundary, and (4) the exact documentation locus. The Coordinator accepted iteration 1, applied free-section refinements, and directed this focused iteration to execute all four threads with synthetic/read-only evidence. No new hypothesis was added.

## Scope Intent

- **In scope:** H2a and H4 only; Full/new/legacy Assisted state fixtures; Windows and explicit macOS/Linux portability; Drive/GitHub/local folder or archive acquisition; exact-object, digest, dynamic-manifest, and immediate recheck semantics; origin-authentication residual risk; minimum documentation ownership.
- **Out of scope:** product edits, HL edits, TS, iteration-control updates, automated download/update runtime, credential handling, publisher signing implementation, private field content, and migration or writes to any real machine-local binding store.

## Guiding Questions

1. Does one common TFW parent with separate implementation-owned files remain safe in every binding fixture, including legacy/new disagreement and POSIX placement?
2. Can Drive, GitHub, and local inputs satisfy one provider-neutral acquisition contract while preserving exact-object, digest, dynamic-manifest, and recheck evidence?
3. What is the honest origin-authentication boundary, and which single document owns each publisher, consumer, migration, and maintainer claim?

## User Direction

- Continue as the same Researcher in focused mode and complete iteration 2 without pausing at checkpoint gates unless a real contract blocker appears.
- Use concrete synthetic/read-only fixtures for all four predecessor threads.
- Write only `research/iter2/**`; do not modify the HL, `status.md`, `iterations.yaml`, TS, or product files.

---
Stage complete: YES
