# Briefing — "What should we investigate?"
> **Mindset:** Strategist. You're planning an investigation, not doing it. Frame what matters. Resist solving.
> **Test:** "Can I explain WHY we're investigating this and what would change our approach?"
> Parent: [HL-TFW_20260830-202031_FA15ES](../../HL-TFW_20260830-202031_FA15ES.md)
> Goal: Restore the complete non-private Assisted 1.6 behavior from the field lineage through an exact preservation-first path and hunk ledger before TS.

## Research Plan

### Gather

- Re-open iteration 2 decisions D1–D9 and the Coordinator's new H1/H3 direction; treat the accepted binding/update decisions as constraints on path and hunk classification, not as product-edit authority.
- Produce a hash-backed census of every one of the 28 read-only field-source files, all source-only/shared/current-only relationships under `editions/02-assisted`, and the two relevant `editions/maintenance` artifacts.
- Decompose disposition, content authority, allowed edit reason, provenance/privacy, migration, and substantial-capability preservation into independent dimensions.
- Inspect source documents without reproducing the four private record bodies; identify private records in traces only by path, category, and exclusion reason, while keeping any source-tree integrity evidence in the read-only inspection rather than per-record trace rows.

### Extract

- Assign every source and current-only path exactly one final disposition and resulting target path: preserve, boundedly adapt, relocate/adapt, exclude, retain neutral current-only, or delete rejected current-only.
- Enumerate every planned source change site using one declared zero-context hunk rule, group it by the six DoD 3 classes, and compute exact per-file and global counts.
- Separate legitimate sanitized 1.0–1.6 provenance from private organization/project facts; preserve functional milestones and migration maps while removing only private subjects, paths, hashes, and overlay-specific facts.
- Verify that each lifecycle algorithm, metadata file, worked template, and artifact-builder capability remains accounted for rather than collapsed into a category summary.

### Challenge

- Attack the ledger for missing/duplicate paths, ambiguous dispositions, unclassified hunks, private leakage, behavior loss, container-name inconsistency, and current-only rejected machinery that accidentally survives.
- Compare source line/range coverage with the proposed hunk inventory and run independent totals by path, class, and target action.
- Test whether each substantial template and all five skill algorithms retain their source structure after only bounded edits; flag any hunk whose replacement meaning cannot be determined without changing a frozen claim.
- Attempt to preserve sanitized provenance while removing private facts from `CHANGELOG.md`, `MIGRATION.md`, examples, navigation, and brand assets.

### Synthesis

- Decide H1 and H3 from the complete ledger and hunk evidence and identify any true ambiguity or amendment proposal.
- State the remaining gap, if any, as `SUFFICIENT`, `MORE NEEDED`, or `BLOCKED`; distinguish TS/implementation verification from unresolved research.
- Classify HL refinements and frozen-section amendment proposals without editing any non-iter3 file.

## Hypotheses (from HL §10)

| # | Hypothesis | HL Status |
|---|-----------|-----------|
| H1 | At least the complete non-private behavioral structure of every field 1.6 Markdown file can be preserved; required edits remain bounded to the five DoD 3 classes. | confirmed — owner answer: “definitely yes” (2026-09-02); exact ledger/hunk evidence pending |
| H3 | The 1.0–1.6 changelog can retain every useful functional and migration milestone while removing private facts and clearly distinguishing overlay provenance. | confirmed — owner requires sanitized Assisted history (2026-09-02); exact hunk classification pending |

## Predecessor Context

Iteration 2 [RES](../iter2/RES.md) confirmed D1–D3: the isolated Assisted child binding path, legacy preserve/ignore behavior, one-time re-onboarding, and explicit POSIX family-root safety boundary. It confirmed D4–D8: provider-neutral closed-tree acquisition, dynamic manifests/rechecks, the A0+A1 origin boundary, publisher-owned shelf documentation, and deletion of the rejected JSON pair. D9 recommended `SUFFICIENT` for H2a/H4.

The Coordinator accepted those decisions, marked H2a/H4 supported, and opened iteration 3 because the master HL still carries two different pre-TS blind spots: the exact 28-file/current-only disposition and the exact neutralization-hunk count for H1/H3. These are new research directions rather than reopened binding/update questions.

## Scope Intent

- **In scope:** all 28 source paths; every current-only `editions/02-assisted` path; relevant `editions/maintenance` JSON paths; path/size/hash/line evidence; final target path/action; zero-context planned hunk ranges and allowed-class totals; privacy/provenance separation; template/algorithm preservation; ambiguity and amendment classification.
- **Out of scope:** reproducing private record contents; product, source, HL, status, iteration-control, TS, or release edits; authoring final replacement prose; changing accepted H2a/H4 decisions; executing publication or migration.

## Guiding Questions

1. Does every source/current-only path have one explicit final action and resulting path, with private content excluded without loss of non-private behavior?
2. Can every required source change site be counted and assigned to an approved DoD 3 class without an unclassified rewrite?
3. Do the complete skills, release lineage, templates, and builder remain substantial after the bounded changes, with no frozen-contract amendment required?

## User Direction

- Continue as the same Researcher in focused mode and close both master-HL blind spots before TS.
- Do not copy private record contents into traces; identify private files/categories and exclusion reasons only.
- Write only `research/iter3/**`; all source, product, HL, status, iteration-control, and TS files remain read-only.
- Complete all checkpoint stages without pausing unless a real contract blocker appears.

---
Stage complete: YES
