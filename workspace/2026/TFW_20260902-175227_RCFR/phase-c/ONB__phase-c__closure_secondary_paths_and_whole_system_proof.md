# ONB — TFW_20260902-175227_RCFR / Phase C: Closure, Secondary Paths, and Whole-System Proof

> **Date**: 2026-09-04
> **Author**: saubakirov (via Codex)
> **Status**: 🟠 ONB — Ready; no blockers
> **Parent HL**: [Phase C HL](HL__phase-c__closure_secondary_paths_and_whole_system_proof.md)
> **TS**: [TS Phase C](TS__phase-c__closure_secondary_paths_and_whole_system_proof.md)

---

## 1. Understanding

Phase C completes the frozen runtime-context reduction contract without changing TFW behavior. I
will first extend the existing source-derived audit so the immutable `cf36dd6` tree and the
candidate expose every primary, secondary, lifecycle, repeated, transitive, and dynamic edge. I
will then make each secondary skill a thin role-aware router, make each canonical workflow the sole
ordered-read and algorithm owner, compact the status/event form carriers, remove only evidenced
stale or competing runtime text, synchronize the existing adapter topology, and finish with the
full semantic, lifecycle, clean-receiver, word, scope, exclusion, and project gates required by the
approved TS.

## 2. Entry Points

- Governing artifacts: the frozen Master HL, Phase C derivation, and approved Phase C TS at
  Coordinator commit `f517c83`; immutable implementation baseline `cf36dd6`.
- Canonical secondary algorithms: `.tfw/workflows/{resume,docs,knowledge,release,update,config,init}.md`.
- Command routers: `.tfw/adapters/codex/skills/tfw-{resume,docs,knowledge,release,update,config,init}/SKILL.md`
  and their installed `.agents/skills/` copies.
- Tracked workflow copies: `.claude/commands/tfw-*.md` and retained singular
  `.agent/workflows/tfw-*.md`; plural `.agents/workflows/` remains the clean-receiver Antigravity
  destination declared by the manifest.
- Lifecycle forms and shared authority: `.tfw/templates/status.md`,
  `.tfw/templates/journal/event.md`, addressed ranges in `.tfw/conventions.md`, and term routes in
  `.tfw/glossary.md`.
- Existing proof machinery: `.tfw/scripts/{gen_index.py,test_gen_index.py}` and
  `docs/scripts/{test_runtime_context.py,test_integration.py}`.

## 3. Questions (blocking — cannot proceed without answers)

No blocking questions. The TS is approved, its baseline resolves, the entry worktree is clean, the
single declared acting handle is `saubakirov`, and the delegated AG execution grant covers the
approved surface.

## 4. Recommendations (suggestions, not blocking)

1. Implement in the TS dependency order: establish AC-1 first; then AC-2/AC-3; then AC-4/AC-5;
   then AC-6; then measure AC-7; finally run AC-8. This keeps each later reduction or parity claim
   dependent on an already source-derived graph.
2. Treat the generated audit and ledger only as evidence output. Tests must independently reject
   missing edges, competing authorities, invalid headings, stale clauses, and output-changing
   mutants so generated data cannot validate itself.
3. Recompute all final counts and quotations after the last implementation and trace change, then
   capture the raw commands directly into the five TS-named evidence transcripts.

## 5. Risks Found (edge cases, potential issues not in TS)

1. The seven baseline workflows express reads in several legacy forms rather than one Read Contract.
   A candidate-only parser would undercount the immutable baseline; the audit must resolve both
   grammars explicitly and fail on unclassified full or transitive loads.
2. Several status/event constraints currently appear after examples and rationale. Compaction must
   put every immutable bound before a workflow can write, while retaining legacy `actor` tolerance
   and the intentionally reserved, currently invalid `consolidation` kind.
3. This worktree began detached at the Coordinator commit. It now has its own Executor branch, and
   every commit will use an explicit path list after a full `git status` read so unrelated or
   concurrently staged work cannot be absorbed.

## 6. Inconsistencies with Code (spec vs reality)

1. Phase state is `TS_DRAFT` while the TS header is approved. This is the expected pre-handoff
   lifecycle: the approved TS is executable authority, and the phase moves to `ONB` only after this
   onboarding record is committed.
2. Current secondary skills independently preload `AGENTS.md`, full conventions, glossary, and
   knowledge inputs before routing to workflows; AC-2 requires the workflow alone to own those
   ordered reads.
3. Current Resume scans obsolete `HL__Phase*`-style filenames, Docs preloads all of `KNOWLEDGE.md`,
   Release/Update read unbounded changelog history, and Config registry rows name unresolved
   `Limits` headings. These are the source-observed defects that the TS explicitly orders Phase C to
   repair, not scope ambiguities.
4. Canonical workflows and their tracked Claude/legacy Antigravity copies, and Codex source skills
   and installed copies, are byte-equal at entry. Any source edit therefore requires same-phase
   synchronization to preserve that invariant.

## 7. Knowledge Citations

| # | HL §7.2 ref | Read? | Applied / N/A | Notes |
|---|-------------|-------|---------------|-------|
| 1 | K1 — Project North Star NS1 | ✅ | Applied | Reduction is accepted only with inspectable authority, state, evidence, and continuation intact. |
| 2 | K2 — Project North Star NS3 | ✅ | Applied | No new documentation layer, generated runtime packet, or artifact-count bureaucracy will be introduced. |
| 3 | K3 — Methodology values | ✅ | Applied | Gates remain structural, names precise, and all durable behavior remains provider-independent. |
| 4 | K4 — philosophy F22, F40, F43, F45 | ✅ | Applied | Templates keep one form job; missing terms/ownership are repaired architecturally; subtraction precedes addition. |
| 5 | K5 — D23, D25, D61 | ✅ | Applied | Existing successful progressive-disclosure and deletion precedents guide compaction without reviving removed review ceremony. |
| 6 | K6 — D63, D68, D72 | ✅ | Applied | Frozen claims are untouched; task/phase state stays local; rung-specific REVISE authority remains conventions-owned. |
| 7 | K7 — Conventions `Design Rules` and `Anti-patterns (prohibited)` | ✅ | Applied | Workflows stay algorithmic and section-addressed; no role-lock, scope, trace, or evidence violation is admitted. |
| 8 | K8 — convention F4, F8, F14 | ✅ | Applied | Every reference sits inside a step, category lists retain one owner, and form templates do not repeat other carriers. |
| 9 | K9 — process F3, F4, F22, F30 | ✅ | Applied | Precise naming plus numbered gates replaces tautology; every surviving rule has an enforcement site. |
| 10 | K10 — PV priorities 7 scan | ✅ | Applied | No new domain/environment constraint changes the approved bound. Two relevant operational items are recorded below. |
| 11 | New P7 — stakeholder F13 | ✅ | Applied | Resume may be compacted and repaired but not deleted; deletion is a separate owner-scoped task. |
| 12 | New P7 — risk F1 | ✅ | Applied | Commits use explicit path lists after reading the full status; broad staging is prohibited for this execution. |
| 13 | Phase P3 — D73, D74 | ✅ | Applied | Phase A/B workflow ownership, digest state, semantic oracle, exact manifest topology, primary totals, and rung routing are preserved. |
| 14 | Phase P6 — process F32, F35, F37–F40, F43 | ✅ | Applied | Final evidence is re-captured at the named candidate, clean receivers remain mandatory, pre-write bounds are exercised, and deliberate absences stay explicit. |

## 8. Return Round 1

### 8.1 Understanding

The live REVIEW at Reviewer commit `b33d534af9767d39052b6a9a6c91533ac519a9f8` returned exactly
three Rung-1 defects. Coordinator commit `7bded933d4c72a6c728722e4c2b3694e8baee7c6` accepted them as
proposed and ruled them paid in this phase. The existing approved TS remains the sole execution
order; previously verified word-count, primary-path, receiver, scope, exclusion, freeze, and
task-local-authority results stay closed.

### 8.2 Questions

No blocking questions. The ruling names the same Executor, exact files and observable completion;
the delegated AG grant authorizes the bounded return.

### 8.3 Accepted Work

1. Replace the Phase C producer that stores expected tuples with six independently derived fields
   from named baseline/candidate clauses; preserve independent expectations, anchor failure, and
   output-changing mutants.
2. Make the actual current-event pre-write gate reject impossible calendar/offset timestamps,
   absolute or task-escaping refs, and non-string, multiline, or over-ceiling summaries while the
   historical reader remains tolerant.
3. Express one baseline-equivalent role for Docs and Release across workflow headings, locks,
   manifest routes, source/installed skills, and tracked copies; replace the fixed-literal role
   scan with a source-derived, mutation-sensitive competing-role census.

### 8.4 Risks and Controls

- Semantic extraction must not move the expected tuple into another production container; each
  field needs its own source rule and provenance before comparison.
- Path checks must use semantic normalization and reject drive/UNC/rooted forms plus `..` escape,
  without imposing the new gate on immutable legacy events.
- Role normalization must retain the existing manifest role (`Coordinator`) and exact adapter copy
  topology; mutation tests must discover declarations rather than search only known conflict text.

### 8.5 Knowledge Citations

The original ONB §7 citations remain applicable and unchanged. D73 governs independent six-field
production, D68 governs immutable-event compatibility, D74 and the Role Lock Protocol govern role
parity, and D72 governs this finite Rung-1 return. No new P7 item changes the accepted bound.

---

*ONB — TFW_20260902-175227_RCFR / Phase C: Closure, Secondary Paths, and Whole-System Proof | 2026-09-04*
