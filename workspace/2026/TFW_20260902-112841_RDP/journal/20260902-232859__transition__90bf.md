---
time: 2026-09-02T23:28:59+05:00
kind: transition
on_behalf_of: saubakirov
via: claude-code
refs:
  - RF__TFW_20260902-112841_RDP.md
  - TS__TFW_20260902-112841_RDP__rev5.md
summary: "round 5 delivered: three corpus figures removed, one outcome corrected, the false universal true"
---

`from: ONB` · `to: RF`

**Round 5's two items are on disk.** Every corpus figure in the unreleased 2.1.0 entry is now true and
re-derivable, or gone: the `116` denominator and this task's own `253 rows` are removed rather than
corrected, and `56 tasks validate` is replaced by the true outcome — the check **exits 1** — with its
command beside it. The suite figure gained its command too, because §7.1 asks for truth **and** a
recoverable command and only the first half was failing visibly. `glossary.md`'s `Session Naming` no
longer claims a universal it does not have, no longer cites a `plan.md` step that does not exist, and
names in one clause why that step is deliberately absent.

**The entry was swept and the sweep's own number is a measurement: 17 quantitative claims**, each with its
author and verdict in RF §6.5. **Suite 322 passed, 1 skipped.** Everything out of scope byte-identical
against `c38f87a`, nothing of mine under `tasks/`, and the journal event untouched — `--check tasks`
still exits 1 on it by the owner's ruling, which the CHANGELOG now names as the reason rather than
reporting as a defect.

**One figure left standing, with the case against leaving it.** `review.md is 1 706 words` is false at
HEAD by seven words and this task's own rounds staled it. Out of bound and recommended for the release
step — ONB §11.5 item 2.

RF appended as subsections `1.5`–`9.5`. Rounds 1–4 untouched, with one ordered exception recorded in
§1.5: §7.4's fact candidate **F18** is corrected in place, because its rule was right and its worked
example refuted it.
